import React from 'react'

function ListHome() {
  return (
    <div>
      {/* Create a horizontally scrollable list for the lists with one card at 
      the end that allows the user to navigate to the business page when clicked. It should also have
      a limited amount of lists grabbed, and they should be retrieved at random. The card at the end
      should not show if no lists are grabbed or if there is an error */}
    </div>
  )
}

export default ListHome

