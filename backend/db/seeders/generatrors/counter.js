const configuration = {
	maxCustomDescriptions: 200,
	maxUsers: 200,
	maxThingsToDo: 1000,
	maxThingsToDoLists: 500,
	maxThingsToDoTags: 120,
	maxExperiences: 3000,
	maxExperiencePhotos: 10000,
	maxBusinesses: 2000,
	maxBusinessJoins: 1000,
	maxThingRatings: 4000,
	maxThingsToDoListTOthingsToDo: 5000,
};

module.exports = configuration;
