const configuration = {
	maxCustomDescriptions: 3000,
	maxUsers: 500,
	maxThingsToDo: 64,
	maxThingsToDoLists: 5000,
	maxThingsToDoTags: 1000,
	maxExperiences: 6000,
	maxExperiencePhotos: 3000,
	maxBusinesses: 500,
	maxBusinessJoins: 2000,
	maxThingRatings: 4000,
	maxThingsToDoListTOthingsToDo: 5000,
};

module.exports = configuration;
